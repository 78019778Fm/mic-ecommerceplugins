spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${LB_AUTH_ISSUER_URI:http://127.0.0.1:9001}
      client:
        registration:
          appmic-e-commerceplugins-maintenance-client:
            provider: spring
            client-id: maintenance-client
            client-secret: 12345
            authorization-grant-type: authorization_code
            redirect-uri: ${LB_MAINTENANCE_URI:http://127.0.0.1:9090}/api/users/authorized
            scope:
              - read
            client-name: appmic-e-commerceplugins-maintenance-client
        provider:
          spring:
            issuer-uri: ${LB_AUTH_ISSUER_URI:http://127.0.0.1:9001}