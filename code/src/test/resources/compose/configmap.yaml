apiVersion: v1
kind: ConfigMap
metadata:
  name: appmic-e-commerceplugins-maintenance
data:
  lb_auth_issuer_uri: "http://127.0.0.1:41215"
  lb_maintenance_uri: "http://127.0.0.1:45819"
  ms-maintenance_name: "appmic-e-commerceplugins-maintenance"
  port: "9090"
  db_host: "mysql:3306"
  database: "maintenance_service"
#  username: root -- Ya se están obteniendo desde el secret
#  password: root_password
  application.yaml: |-
    config:
      texto: Configurando ambiente por defecto
    ---
    spring:
      config:
        activate:
          on-profile: dev
    config:
      texto: Configurando ambiente de desarrollo
    ---
    spring:
      config:
        activate:
          on-profile: prod
    config:
      texto: Configurando ambiente de produccion
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: appmic-e-commerceplugins-orders
data:
  port: "9091"
  db_host: "mysql:3306"
  database: "orders_service"
#  username: root
#  password: root_password
